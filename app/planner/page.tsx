import { SearchForm } from "@/components/search-form";
import { aiPrompts } from "@/lib/ai/prompts";
export default function PlannerPage(){return <main className="premium-gradient min-h-screen px-4 py-12"><div className="mx-auto max-w-5xl"><h1 className="text-5xl font-black text-navy">AI trip planner</h1><p className="mt-4 text-lg text-slate-600">Plan full itineraries with budget optimization, alternative destinations, risk analysis, and package-aware recommendations.</p><div className="mt-8"><SearchForm/></div><div className="mt-8 rounded-[2rem] bg-white p-6 shadow-premium"><h2 className="text-2xl font-bold text-navy">Internal prompt library</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{Object.entries(aiPrompts).map(([k,v])=><div key={k} className="rounded-2xl border p-4"><b className="capitalize">{k.replace(/([A-Z])/g," $1")}</b><p className="mt-2 text-sm text-slate-600">{v}</p></div>)}</div></div></div></main>}
